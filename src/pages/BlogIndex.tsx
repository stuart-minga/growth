import { Prose } from '../components/ui/Prose'

export function BlogIndex() {
  return (
    <main className="py-section">
      <Prose>
        <h1>Blog</h1>
        <p className="text-lg text-ink-muted">
          Chronological writing will appear here once posts are wired in.
        </p>
      </Prose>
    </main>
  )
}
